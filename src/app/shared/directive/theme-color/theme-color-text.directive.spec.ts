import { ThemeColorTextDirective } from './theme-color-text.directive';

describe('ThemeColorTextDirective', () => {
  it('should create an instance', () => {
    // TODO 実装
    // const directive = new ThemeColorTextDirective();
    // expect(directive).toBeTruthy();
  });
});
