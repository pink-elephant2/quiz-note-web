import { ThemeColorBgDirective } from './theme-color-bg.directive';

describe('ThemeColorBgDirective', () => {
  it('should create an instance', () => {
    // TODO 実装
    // const directive = new ThemeColorBgDirective();
    // expect(directive).toBeTruthy();
  });
});
