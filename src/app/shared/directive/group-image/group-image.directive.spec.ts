import { GroupImageDirective } from './group-image.directive';

describe('GroupImageDirective', () => {
  it('should create an instance', () => {
    // TODO 実装
    // const directive = new GroupImageDirective();
    // expect(directive).toBeTruthy();
  });
});
